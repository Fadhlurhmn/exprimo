name: exprimo
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  path_provider: ^2.1.5
  dio: ^5.7.0
  permission_handler: ^11.3.1
  crypto: ^3.0.0
  photo_view: ^0.15.0
  flutter_downloader: ^1.8.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  path: ^1.9.0
  firebase_core: ^3.6.0
  firebase_database: ^11.1.4
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2
  firebase_storage: ^12.3.5
  image: ^4.0.15
  intl: ^0.19.0 # Menurunkan versi intl untuk menyelesaikan konflik dengan firebase_auth
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  firebase_ml_model_downloader: ^0.3.1+5
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
