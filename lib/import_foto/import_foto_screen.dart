import 'package:flutter/material.dart';

class importFotoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Ini Page menu Import foto'),
    );
  }
}
