import 'package:flutter/material.dart';

class FaceQuestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Ini Page menu face quest'),
    );
  }
}
